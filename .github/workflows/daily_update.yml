name: GeoPuls Daily AI Update

on:
  schedule:
    - cron: '0 4 * * *'  # Täglich 04:00 UTC
  workflow_dispatch:     # Manuell auslösbar

jobs:
  run-ai:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Code auschecken
        uses: actions/checkout@v5

      - name: Python einrichten
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Groq-SDK und Feedparser installieren
        run: |
          pip install groq feedparser

      - name: KI Analyse ausführen
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          python update_dashboard.py

      - name: data.json committen und pushen
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data.json
          git commit -m "GeoPuls: Tägliches KI-Update [skip ci]" || exit 0
          git push
